fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.0
# Required by Starlette's SessionMiddleware for cookie signing.
# Not pulled in automatically by fastapi; must be explicit.
itsdangerous==2.2.0
# yt-dlp and its extras (yt-dlp-ejs via [default], curl_cffi via [curl-cffi])
# are installed in the Dockerfile via `yt-dlp[default,curl-cffi]==<pin>`. The
# Dockerfile is the single source of truth for those versions — do NOT add
# curl_cffi or yt-dlp-ejs here, as that creates two competing version pins.
# See Dockerfile YTDLP_VERSION ARG.
