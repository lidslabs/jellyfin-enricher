# Secrets — never commit
.env

# Runtime state — local to deployments, not source
data/
*.db
*.db-journal

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Private AI-agent docs — persistent project memory, never pushed.
# CLAUDE.md is the session-start operating card; .project/ is the manual.
# Convention to mirror across all lidslabs repos (jellyfin-hdr, media-utils).
/CLAUDE.md
.project/
